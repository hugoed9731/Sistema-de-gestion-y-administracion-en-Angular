import { FilterpaseoactivoPipe } from './filterpaseoactivo.pipe';

describe('FilterpaseoactivoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterpaseoactivoPipe();
    expect(pipe).toBeTruthy();
  });
});
