import { FilterBancoPipe } from './filter-banco.pipe';

describe('FilterBancoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBancoPipe();
    expect(pipe).toBeTruthy();
  });
});
