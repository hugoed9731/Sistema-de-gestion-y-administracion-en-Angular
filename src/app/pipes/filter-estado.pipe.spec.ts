import { FilterEstadoPipe } from './filter-estado.pipe';

describe('FilterEstadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEstadoPipe();
    expect(pipe).toBeTruthy();
  });
});
