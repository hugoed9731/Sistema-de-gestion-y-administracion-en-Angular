import { FilterFechaPipe } from './filter-fecha.pipe';

describe('FilterFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
