import { FilterfinalizadoPipe } from './filterfinalizado.pipe';

describe('FilterfinalizadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterfinalizadoPipe();
    expect(pipe).toBeTruthy();
  });
});
