import { FiltergnombrePipe } from './filtergnombre.pipe';

describe('FiltergnombrePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltergnombrePipe();
    expect(pipe).toBeTruthy();
  });
});
