import { FilteruidordPipe } from './filteruidord.pipe';

describe('FilteruidordPipe', () => {
  it('create an instance', () => {
    const pipe = new FilteruidordPipe();
    expect(pipe).toBeTruthy();
  });
});
