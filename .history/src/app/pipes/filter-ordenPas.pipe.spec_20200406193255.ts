import { FilterClaveibPipe } from './filter-claveib.pipe';

describe('FilterClaveibPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterClaveibPipe();
    expect(pipe).toBeTruthy();
  });
});
